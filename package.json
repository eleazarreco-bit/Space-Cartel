{
  "name": "car-keys-server",
  "version": "1.0.0",
  "description": "Car Keys — shared daily BAN/CTN assignment tool with a Postgres-backed API, so multiple agents on different devices see consistent state.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.23.0"
  }
}
