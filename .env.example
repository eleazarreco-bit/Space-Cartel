PORT=3000
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
# Set to false only for local Postgres without SSL. Leave unset (or true)
# for hosted providers like Supabase/Neon which require SSL over the internet.
# PGSSL=false
